<%- include('../layout2/header') -%>
<div class="screen-overlay"></div>
<aside class="navbar-aside" id="offcanvas_aside">
    <div class="aside-top">
        <a href="index.html" class="brand-wrap">
            <img src="/admin/assets/imgs/card-brands/croped.png" alt="Evara Dashboard" style="width: 180px; padding-left: 40px;padding-top: 10px;">
        </a>
        <div>
            <button class="btn btn-icon btn-aside-minimize"> <i class="text-muted material-icons md-menu_open"></i> </button>
        </div>
    </div>
    <nav>
        <ul class="menu-aside">
            <li class="menu-item ">
                <a class="menu-link" href="/admin/dashboard"> <i class="icon material-icons md-home"></i>
                    <span class="text">Dashboard</span>
                </a>
            </li>
            <li class="menu-item">
                <a class="menu-link" href="/admin/users"> <i class="icon material-icons md-stars"></i>
                    <span class="text">Users</span> </a>
            </li>
            <li class="menu-item active">
                <a class="menu-link" href="/admin/category"> <i class="icon material-icons md-stars"></i>
                    <span class="text">Categories</span> </a>
            </li>
            <li class="menu-item ">
                <a class="menu-link" href="/admin/productlist"> <i class="icon material-icons md-shopping_bag"></i>
                    <span class="text">Products</span>
                </a>
                
            </li>
            <li class="menu-item ">
                <a class="menu-link" href="/admin/orders"> <i class="icon material-icons md-shopping_cart"></i>
                    <span class="text">Orders</span>
                </a>
                
            </li>
            <li class="menu-item ">
                <a class="menu-link" href="/admin/coupon"> <i class="icon material-icons md-shopping_cart"></i>
                    <span class="text">Coupon</span>
                </a>
                
            </li>
            <li class="menu-item ">
                <a class="menu-link" href="/admin/offer"> <i class="icon material-icons md-shopping_cart"></i>
                    <span class="text">Offer</span>
                </a>
                
            </li>
            <li class="menu-item ">
                <a class="menu-link" href="/admin/banner"> <i class="icon material-icons md-shopping_cart"></i>
                    <span class="text">Banner</span>
                </a>
                
            </li>
            
        </ul>
        <hr>
       
        <br>
        <br>
    </nav>
</aside>
    <main class="main-wrap">
        <header class="main-header navbar">
            <div class="col-search">
                <form class="searchform">
                    <div class="input-group">
                        <!-- Correct the ID here -->
                        <input id="searchInput" list="search_terms" type="text" class="form-control" placeholder="Search term">
                        <button class="btn btn-light bg" type="button"> <i class="material-icons md-search"></i></button>
                    </div>
                    <datalist id="search_terms">
                        <option value="Products">
                        <option value="New orders">
                        <option value="Apple iphone">
                        <option value="Ahmed Hassan">
                    </datalist>
                </form>
            </div>
            <div class="col-nav">
                <button class="btn btn-icon btn-mobile me-auto" data-trigger="#offcanvas_aside"> <i class="material-icons md-apps"></i> </button>
                <ul class="nav">
                    <li class="nav-item">
                        <a class="nav-link btn-icon" href="#">
                            <i class="material-icons md-notifications animation-shake"></i>
                            <span class="badge rounded-pill">3</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn-icon darkmode" href="#"> <i class="material-icons md-nights_stay"></i> </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="requestfullscreen nav-link btn-icon"><i class="material-icons md-cast"></i></a>
                    </li>
                    <li class="dropdown nav-item">
                        <a class="dropdown-toggle" data-bs-toggle="dropdown" href="#" id="dropdownLanguage" aria-expanded="false"><i class="material-icons md-public"></i></a>
                        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownLanguage">
                            <a class="dropdown-item text-brand" href="#"><img src="assets/imgs/theme/flag-us.png" alt="English">English</a>
                            <a class="dropdown-item" href="#"><img src="assets/imgs/theme/flag-fr.png" alt="Français">Français</a>
                            <a class="dropdown-item" href="#"><img src="assets/imgs/theme/flag-jp.png" alt="Français">日本語</a>
                            <a class="dropdown-item" href="#"><img src="assets/imgs/theme/flag-cn.png" alt="Français">中国人</a>
                        </div>
                    </li>
                    <li class="dropdown nav-item">
                        <a class="dropdown-toggle" data-bs-toggle="dropdown" href="#" id="dropdownAccount" aria-expanded="false"> <img class="img-xs rounded-circle" src="assets/imgs/people/avatar2.jpg" alt="User"></a>
                        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownAccount">
                            <a class="dropdown-item" href="#"><i class="material-icons md-perm_identity"></i>Edit Profile</a>
                            <a class="dropdown-item" href="#"><i class="material-icons md-settings"></i>Account Settings</a>
                            <a class="dropdown-item" href="#"><i class="material-icons md-account_balance_wallet"></i>Wallet</a>
                            <a class="dropdown-item" href="#"><i class="material-icons md-receipt"></i>Billing</a>
                            <a class="dropdown-item" href="#"><i class="material-icons md-help_outline"></i>Help center</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item text-danger" href="/admin/logout"><i class="material-icons md-exit_to_app"></i>Logout</a>
                        </div>
                    </li>
                </ul>
            </div>
        </header>
        <section class="content-main">
            <div class="content-header">
                <div>
                    <h2 class="content-title card-title">Categories </h2>
                    <p>Add, list and unlist Categories</p>
                </div>
                <!-- <div>
                    <input type="text" placeholder="Search Categories" class="form-control bg-white">
                </div> -->
            </div>
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-8">
                            <div class="table-responsive">
                                <table class="table table-hover" id="catatable">
                                    <thead>
                                        <tr>
                                            <th class="col">Index</th>
                                            <th class="col">Category</th>
                                            <th class="col">Description</th>
                                            <th class="col">Status</th>
                                            <th class="col">Offer</th>
                                            <th class="col text-end">Action</th>
                                            
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <!-- <% if ((locals.category).length > 0) { %>
                                            <% for (let i = 0; i < category.length; i++) { %>
                                                <tr>
                                                    <td class="col"><%= category[i].Category %></td>
                                                    <td class="col"><%= category[i].Description %></td>
                                                    <td class="col bg" "><span class="badge rounded-pill <%= category[i].Status === 'active' ? 'alert-success' : 'alert-warning' %>">
                                                        <%= category[i].Status  %>
                                                    </span>
                                                        
                                                    </td>
                                                    <td class="col text-end">
                                                        <a href="/admin/addcata?id=<%= category[i]._id  %>" class="btn btn-sm font-sm btn-warning rounded">
                                                            <i class="material-icons"  ></i> Edit
                                                        </a>
                                                        <% if (category[i].Status==='active'){ %>
                                                            <a href='' class="btn btn-sm font-sm btn-danger rounded">
                                                                <i class="material-icons"></i> Block
            
                                                            </a>
                                                            <% } else{ %>
            
                                                                <a href="" class="btn btn-sm font-sm btn-success rounded">
                                                                    <i class="material-icons"></i> Unblock
                                                                </a>
            
                                                                <% } %>
                                                    </td>
                                                </tr>
                                            <% } %>
                                        <% } %> -->

                                        <% if ((locals.category).length > 0) { %>
                                            <% let i=skip+1 %>
                                            <% category.forEach((category,index)=>{ %>
                                                <tr class="<%= index % 2 === 0 ? 'even-row' : 'odd-row' %>">
                                                    <td class="col"><%= i++ %></td>
                                                    <td class="col"><%= category.Category %></td>
                                                    <td class="col" style="width: 200px; word-wrap: break-word"><%= category.Description %></td>
                                                    <td class="col bg" ">
                                                        <span class="badge rounded-pill <%= category.Status === 'Active' ? 'alert-success' : 'alert-warning' %>">
                                                        <%= category.Status  %>
                                                        </span>
                                                        
                                                    </td>

                                                    <% if (category.offer) { %>
                                                        <td>  <button id="removeofferButton" class="rounded-pill" onclick="removeoffer('<%= category._id %>')">Remove</button><br>
                                                            <span id="offerNameSpan" ><%= category.offer.name %></span></td>
                                                        
                                                    <% } else { %>
                                                        <td>  <button id="offerButton" class="rounded-pill" onclick="openModal('<%= category._id %>')">Offer</button></td>
                                                    <% } %>


                                                    <td class="col text-end">
                                                        <a href="/admin/addcata?id=<%= category._id  %>" class="btn btn-sm font-sm btn-warning rounded">
                                                            <i class="material-icons"  ></i> Edit
                                                        </a>
                                                        <% if (category.Status==='Active'){ %>
                                                            <a href='/admin/catablock?id=<%= category._id  %>' class="btn btn-sm font-sm btn-danger rounded actionButton">
                                                                <i class="material-icons"></i> Block
            
                                                            </a>
                                                            <% } else{ %>
            
                                                                <a href="/admin/cataunblock?id=<%= category._id  %>" class="btn btn-sm font-sm btn-success rounded actionButton">
                                                                    <i class="material-icons"></i> Unblock
                                                                </a>
            
                                                                <% } %>
                                                                <a href="/admin/catadelete?id=<%= category._id  %>" class="btn btn-sm font-sm btn-dark rounded deleteButton">
                                                                    <i class="material-icons"  ></i> Delete
                                                                </a>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        
                        <% if (locals.cata) { %>
                            <div class="col-md-4">
                                <form action="/admin/updatecata"  method="POST"  onsubmit="return validateForm()">
                                    <div class="mb-4">
                                        <label for="product_name" class="form-label" >Name</label>
                                        <input type="text" placeholder="Type here" class="form-control" name="category" id="product_name" value=" <%= cata.Category %>"  required pattern="[A-Za-z\s]+" title="Only letters are allowed">
                                    </div>
                                    <div class="mb-4">
                                        <label class="form-label">Status</label>
                                        <select class="form-select" name="status" required>
                                            <% if(cata.Status==='Active') { %>
                                                <option value="Active" selected>Active</option>
                                                <option value="Blocked">Blocked</option>
                                                <% } else { %>
                                                    <option value="Active" >Active</option>
                                                    <option value="Blocked" selected>Blocked</option>
                                                    <% } %>
                                          
                                        </select>
                                    </div>
                                    <div class="mb-4">
                                        <label class="form-label">Description</label>
                                        <textarea placeholder="Type here" class="form-control" name="Description" required><%= cata.Description %></textarea>
                                    </div>
                                    <% if (locals.message) { %>
                                        <p id="alert"><%= message %></p>
                                    <% } %>
                                    <div class="d-grid">
                                        <input type="hidden" name="id" value="<%=cata._id %>" >
                                        <button class="btn btn-primary">Update category</button>
                                    </div>
                                </form>
                                <div class="d-grid">
                                    <input type="hidden" name="id" value="<%=cata._id %>" >
                                    <button class="btn btn-secondary w-50 align-items-center" style="margin-top: 20px; margin-left: 100px;">
                                      <a href="/admin/category"  style="color: white;">Cancel</a>
                                    </button>
                                  </div>
                            </div>
                        <% } else { %>

                                <div class="col-md-4">
                                    <form action="/admin/category"  method="POST"  onsubmit="return validateForm()">
                                        <div class="mb-4">
                                            <label for="product_name" class="form-label">Name</label>
                                            <input type="text" placeholder="Type here" class="form-control" name="name" id="product_name" required pattern="[A-Za-z\s]+" title="Only letters are allowed"/>
                                        </div>
                                        <div class="mb-4">
                                            <label class="form-label">Status</label>
                                            <select class="form-select" name="status"required>
                                                <option value="Active">Active</option>
                                                <option value="Blocked">Blocked</option>
                                            </select>
                                        </div>
                                        <div class="mb-4">
                                            <label class="form-label">Description</label>
                                            <textarea placeholder="Type here" class="form-control" name='Description'required></textarea>
                                        </div>
                                        <% if (locals.message) { %>
                                            <p id="alert"><%= message %></p>
                                        <% } %>
                                        <div class="d-grid">
                                            <button class="btn btn-primary">Create category</button>
                                        </div>
                                    </form>
                                </div>
                                <% } %>
                        
                        
                    </div> <!-- .row // -->
                </div> <!-- card body .// -->
                
            </div> <!-- card .// -->
            
        </section> <!-- content-main end// -->
       

        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <% if (currentPage > 1) { %>
                    <li class="page-item">
                        <a class="page-link" href="/admin/category?page=<%= currentPage - 1 %>" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                <% } else { %>
                    <li class="page-item disabled">
                        <span class="page-link" aria-hidden="true">&laquo;</span>
                    </li>
                <% } %>
        
                <% for (let page = 1; page <= totalpage; page++) { %>
                    <li class="page-item <%= currentPage === page ? 'active' : '' %>">
                        <a class="page-link" href="/admin/category?page=<%= page %>"><%= page %></a>
                    </li>
                <% } %>
        
                <% if (currentPage < totalpage) { %>
                    <li class="page-item">
                        <a class="page-link" href="/admin/category?page=<%= currentPage + 1 %>" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                <% } else { %>
                    <li class="page-item disabled">
                        <span class="page-link" aria-hidden="true">&raquo;</span>
                    </li>
                <% } %>
            </ul>
        </nav>





        <% if (locals.offer) { %>
            <!-- Modal HTML -->
            <div id="myModal" class="modal">
                <button onclick="closeModal()" class="close-button">&times;</button><br><br>
                <div class="modal-header">
                    
                </div>
                <div>
                    <% locals.offer.forEach((offer) => { %>
                        <div class="offerDetails">
                            <h2 class="offerHeading">Special Offer: <%= offer.name %></h2>
                            <p class="offerPercentage"><strong>Discount:</strong> <%= offer.percentage %>%</p>
                            <p class="offerStartDate">
                                <strong>Valid from:</strong> <%= offer.startDate %> to <%= offer.endDate.toLocaleDateString() %>
                            </p>
                       
                            
                            <input type="hidden" class="cataInput" value="">
        
                            <button class="offerButton" onclick="yourButtonFunction('<%= offer._id %>')">APPLY</button>
                        </div>
                        <hr class="offerSeparator">
                    <% }) %>
                </div>
            </div>
        <% } %>
        













        <footer class="main-footer font-xs">
            <div class="row pb-30 pt-15">
                <div class="col-sm-6">
                    <script>
                    document.write(new Date().getFullYear())
                    </script> ©, Evara - HTML Ecommerce Template .
                </div>
                <div class="col-sm-6">
                    <div class="text-sm-end">
                        All rights reserved
                    </div>
                </div>
            </div>
        </footer>
    </main>
    <script src="assets/js/vendors/jquery-3.6.0.min.js"></script>
    <script src="assets/js/vendors/bootstrap.bundle.min.js"></script>
    <script src="assets/js/vendors/select2.min.js"></script>
    <script src="assets/js/vendors/perfect-scrollbar.js"></script>
    <script src="assets/js/vendors/jquery.fullscreen.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- Main Script -->
    <script src="assets/js/main.js" type="text/javascript"></script>
    
   
    <script>
        function validateForm() {
            var categoryName = document.getElementById('product_name').value.trim();
            var description = document.getElementsByName('Description')[0].value;
    
            // Add your custom validation logic here
            if (categoryName === '') {
                alert('Name cannot be empty');
                return false;
            }
    
            // Add additional validation as needed
    
            return true; // Form will be submitted if all validations pass
        }
        
    // Correct the ID here as well
    const searchInput = document.getElementById('searchInput');
    const tableRows = document.querySelectorAll('tbody tr');

    searchInput.addEventListener('input', function () {
        const searchTerm = searchInput.value.toLowerCase();

        tableRows.forEach(row => {
            const userName = row.querySelector('td:nth-child(1)').textContent.toLowerCase();
            row.style.display = userName.includes(searchTerm) ? '' : 'none';
        });
    });


               var actionButtons = document.querySelectorAll('.actionButton');

               // Add a click event listener to each action button
               actionButtons.forEach(function (actionButton) {
               actionButton.addEventListener('click', function (event) {
               // Prevent the default behavior of the link
               event.preventDefault();

               // Determine the action type based on the button class
               var actionType = actionButton.classList.contains('btn-danger') ? 'block' : 'unblock';

               // Show a SweetAlert confirmation dialog
                Swal.fire({
                title: 'Are you sure?',
                text: `Do you want to ${actionType} this category?`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: `Yes, ${actionType} it!`
              
            }).then((result) => {
                // If the user confirms, proceed with the action
                if (result.isConfirmed) {
                    window.location.href = actionButton.getAttribute('href');
                }
            });
        });
    });
//soft delete ------
    var deleteButtons = document.querySelectorAll('.deleteButton');

// Add a click event listener to each delete button
deleteButtons.forEach(function (deleteButton) {
    deleteButton.addEventListener('click', function (event) {
        // Prevent the default behavior of the link
        event.preventDefault();

        // Show a SweetAlert confirmation dialog
        Swal.fire({
            title: 'Are you sure?',
            text: 'You are about to delete this category. This action cannot be undone.',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            // If the user confirms, proceed with the deletion action
            if (result.isConfirmed) {
                window.location.href = deleteButton.getAttribute('href');
            }
        });
    });
});







//offer  modal


function openModal(cataId) {
    var modal = document.getElementById("myModal");

console.log(cataId)

var productIdInput = modal.querySelector('.cataInput');
productIdInput.value = cataId 

modal.style.display = "flex";


}




function closeModal() {
    var modal = document.getElementById("myModal");
    modal.style.display = "none";
}





function yourButtonFunction(offerId) {
    var modal = document.getElementById("myModal");

    
    // var offerId = modal.querySelector('.offerIdInput').value;
    var cataId = modal.querySelector('.cataInput').value;

    console.log(offerId,cataId)



    fetch('/admin/cataofferapply', {
        method: 'POST', 
        headers: {
            'Content-Type': 'application/json', 
        },
        body: JSON.stringify({
            offerId: offerId,
            cataId: cataId,
        }),
    })
    .then(response => response.json())
    .then(data => {
        if(data.success) {
          
            
            Swal.fire({
            title: 'Success!',
            text: data.message,
            icon: 'success',
            showCancelButton: false,
            showConfirmButton: false,
            timer: 1000,
   
        })
            .then(() => {
            
                location.reload();
            });
        } else {
            Swal.fire({
            title: 'Error!',
            text: data.message,
            icon: 'error',
            showCancelButton: false,
            showConfirmButton: false,
            timer: 1000,
        
        })

            .then(() => {
            
            location.reload();
        });
        }
        console.log('Response:', data);
    })
    .catch(error => {
       
        Swal.fire({
            title: 'Error!',
            text: 'Failed to apply offer',
            icon: 'error',
            showCancelButton: false,
            showConfirmButton: false,
            timer: 1000,
            
        })
        .then(() => {
            
            location.reload();
        });
        console.error('Error:', error);
    });
}








//remove offer

function removeoffer(cataId) {

fetch('/admin/cataremoveoffer', {
    method: 'POST', 
    headers: {
        'Content-Type': 'application/json', 
    },
    body: JSON.stringify({
        id: cataId
    }),
})
.then(response => response.json())
.then(data => {
    if(data.success) {
      
        
        Swal.fire({
        title: 'Success!',
        text: data.message,
        icon: 'success',
        showCancelButton: false,
        showConfirmButton: false,
        timer: 1000,

    })
        .then(() => {
        
            $('#catatable').load(`/admin/category #catatable`)
        });
    } else {
        Swal.fire({
        title: 'Error!',
        text: data.message,
        icon: 'success',
        showCancelButton: false,
        showConfirmButton: false,
        timer: 1000,
    
    })

        .then(() => {
        
            $('#catatable').load(`/admin/category #catatable`)
    });
    }
    console.log('Response:', data);
})
.catch(error => {
   
    Swal.fire({
        title: 'Error!',
        text: 'Internal server error',
        icon: 'success',
        showCancelButton: false,
        showConfirmButton: false,
        timer: 1000,
        
    })
    .then(() => {
        
        $('#catatable').load(`/admin/category #catatable`)
    });
    console.error('Error:', error);
});

}













    </script>

    <%- include('../layout2/footer') -%>